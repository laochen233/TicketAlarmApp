name: Build Debug APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4

      - name: 安装 JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 安装 Android SDK
        run: |
          set -e
          export ANDROID_HOME="$HOME/android-sdk"
          mkdir -p "$ANDROID_HOME/cmdline-tools"
          cd "$ANDROID_HOME/cmdline-tools"
          if [ ! -d "latest" ]; then
            wget --tries=2 --timeout=300 -O cmdline-tools.zip \
              https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
            unzip -q cmdline-tools.zip
            [ -d cmdline-tools ] && mv cmdline-tools latest || true
          fi
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$ANDROID_HOME/platform-tools" >> $GITHUB_PATH

      - name: 接受协议 + 装平台与构建工具
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: 准备 Gradle 8.10.2
        run: |
          set -e
          GRADLE_DIR="/opt/gradle-8.10.2"
          if [ ! -d "$GRADLE_DIR" ]; then
            echo "下载 Gradle 8.10.2..."
            cd /tmp
            wget --tries=3 --timeout=300 -O gradle-8.10.2-bin.zip \
              https://services.gradle.org/distributions/gradle-8.10.2-bin.zip
            ls -lh gradle-8.10.2-bin.zip
            unzip -q gradle-8.10.2-bin.zip -d /opt/
          fi
          echo "✅ Gradle 解压完成"
          ls "$GRADLE_DIR/bin/gradle"
          echo "$GRADLE_DIR/bin" >> $GITHUB_PATH

      - name: 编译 Debug APK
        run: |
          cd "$GITHUB_WORKSPACE"
          export GRADLE_OPTS="-Xmx3072m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8"
          # 关键:不能直接用 | tail,会吞掉 gradle 退出码
          set -o pipefail
          gradle :app:assembleDebug --no-daemon --stacktrace 2>&1 | tail -500

      - name: 验证 APK 是否生成(用 || true 兜底)
        run: |
          echo "=== 查找 APK ==="
          find app/build/outputs/apk -name "*.apk" -type f 2>/dev/null || echo "(本步未找到)"
          echo "=== debug 目录 ==="
          ls -la app/build/outputs/apk/debug/ 2>/dev/null || echo "(debug 目录不存在)"
          echo "✅ 验证步骤结束(APK 存在与否交给下一步 upload-artifact)"

      - name: 上传 APK 产物
        uses: actions/upload-artifact@v4
        with:
          name: ticket-alarm-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
